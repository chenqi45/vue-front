import {
	CHANGE_OPTIONS
} from '../types'

const state = {item: {pageNo: 1, pageSize: 10}}

const mutations = {
  [CHANGE_OPTIONS](state, action) {
    state.item = {...state.item, ...action.options}
  }
}

export default {
  state,
  mutations
}
